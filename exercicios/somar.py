# Faça um loop para somar até 100

entrada = int(input('Você quer que o programa conte até que número?: '))
pular = int(input('De quanto em quanto?: '))

valor = 0 

while valor <= entrada:
    print(valor)
    valor += pular

